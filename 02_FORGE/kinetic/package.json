{
  "name": "@camelot/kinetic",
  "version": "0.1.0",
  "private": true,
  "description": "Camelot Kinetic Rust binaries (cribo, rotel, pmcp, hephaestus, etc.)",
  "scripts": {
    "cargo:build": "cargo build",
    "cargo:test": "cargo test",
    "cargo:lint": "cargo clippy -- -D warnings",
    "clean": "cargo clean"
  }
}
