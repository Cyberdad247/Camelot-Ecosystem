[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "ouroboros_engine"
requires-python = ">=3.9"
dynamic = ["version"]

[tool.maturin]
features = ["pyo3"]
module-name = "ouroboros_engine"
