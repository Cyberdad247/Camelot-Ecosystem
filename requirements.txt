# Secret Photo Viewer — production dependencies
# Generated from the current working environment. Pin versions for reproducible
# deployments.

fastapi>=0.95.0
uvicorn>=0.22.0
gradio>=6.14.0
pydantic>=2.0
Pillow>=10.0.0

# Monitoring
prometheus-client>=0.20.0

# Password hashing
bcrypt>=4.0.0

# Multi-replica backends
redis>=5.0.0
boto3>=1.34.0

# Google Sign-In / Photos integration
# Optional: used only when SPV_GOOGLE_CLIENT_ID is configured.
google-auth>=2.22.0

# Development / testing
pytest>=7.0.0
httpx>=0.25.0
playwright>=1.40.0
moto[s3]>=5.0.0
