# Hardware run — 2026-08-08 on vm

Phases 2-4A release-gate record — merge/tag gate for PR #200 (camelot-kickbox-voice-v0.2.0) and prerequisite for Phase 4B.

| Field | Value |
|---|---|
| Host | vm (Linux 6.18.5-fc-v20 x86_64) |
| CPU | Intel(R) Xeon(R) Processor @ 2.80GHz × 4 |
| RAM | 15.7 GB |
| GPU | none detected |
| Voice | ENABLE_HERMES_VOICE=true |
| Mesh | ENABLE_TAILSCALE_MESH=true |
| Local-only invariant (item 1) | PASSED |
| Mesh gate probes (items 3-7) | PASSED |
| Idle RSS | gateway=16.8MB node-agent=3.4MB hermes=59.2MB console=19.1MB  |
| Fallback latency | 37ms (dead provider, 1s timeout, until fallback recorded) |
| Memory pressure | MemAvailable 15434MB -> 15442MB (delta -8MB); no swap configured |
| Smoke | PASSED |
| Gate coverage | COMPLETE (voice + mesh both exercised) |

## Item 1 + invariant -- local-only (mesh forced off, CAMELOT_NODE_ID set)
```
── local-only invariant (mesh forced off, CAMELOT_NODE_ID=nondefault-local-id)
   ✔ agent reports an empty nodeId (binding not required)
   ✔ gateway registry is empty (no enrolment happened)
   ✔ smoke passed (16 checks) — Anya Console: http://localhost:8080/kickbox/
── local-only invariant: PASS
```

## Full-stack smoke
```
── 1. Health
   ✔ gateway healthy
   ✔ node-agent healthy (backend: cpu)
   ✔ console + contracts served
── 2. Tier-1 read (no lease)
   ✔ allowed without lease, artifact: staging_status
── 3. Tier-2 draft (auto-lease, consumed)
   ✔ draft created under consumed lease lease-0001
── 4. Tier-3 requires confirmation
   ✔ blocked with pending lease lease-0002
   ✔ approved -> executed -> lease consumed
── 5. Barge-in revokes unused lease
   ✔ barge-in revoked lease-0003; later approval rejected (409)
── 6. Audit trail (redacted, chained)
   ✔ audit audit-1273 fetched with hash chain
── 7. Node-agent compute under signed lease
   ✔ batched compute on backend: cpu
   ✔ forged compute token rejected (403)
   ✔ another node's validly-signed lease rejected (403)
── 8. Model routing (deterministic default)
   ✔ replies narrated by 'deterministic' (requests=1, fallbacks=0)
── 9. Hermes voice adapter (ENABLE_HERMES_VOICE=true)
   ✔ hermes healthy (stt: fixture)
   ✔ stt transcribed speech-energy audio: read staging status
   ✔ silence yields no transcript (nothing submittable)
   ✔ tts produced a WAV stream
── 10. Mesh node registry (ENABLE_TAILSCALE_MESH=true)
   ✔ 1 node(s) enrolled
   ✔ local node trusted; mesh backend: none
   ✔ node list carries no addresses or key material
   ✔ local-first job served under a node lease (audit audit-1278)
   ✔ unregistered node refused
   ✔ cross-tenant job refused

✅ smoke passed (23 checks) — Anya Console: http://localhost:8080/kickbox/
```

## Items 3-7 -- mesh gate probes
```
── Register a synthetic remote node (dead dispatch URL)
   ✔ remote node enrolled as 'pending' (registering is not trust)
── 3. Pending node is reachable but cannot receive a job
   ✔ pending node refused: node trust band does not permit this job: node is pending operator approval
── 4. Lease binding failures
   ✔ wrong tenant refused
   ✔ unregistered capability refused
   ✔ expired lease refused (403)
   ✔ lease is single-use at the node (first 200, replay 403)
── 5. Trusted remote read-only failure falls back to local
   ✔ dead remote → local fallback served the read-only job
── 6. Effectful remote failure is not retried and not re-run locally
   ✔ effectful job failed closed on the remote, no local re-run
   ✔ failed dispatch revoked its lease (audit audit-1297)
── 7. Revocation cuts new work immediately
   ✔ revoked node refused immediately
   ✔ band is terminal 'revoked'

mesh gate probes: 11 passed, 0 failed
```

## Status snapshot
```
✔ gateway  pid=6898  healthy  http://localhost:8788/healthz
✔ node-agent  pid=6920  healthy  http://localhost:8789/healthz
✔ hermes  pid=6935  healthy  http://localhost:8790/healthz
✔ console  pid=6956  healthy  http://localhost:8080/kickbox/index.html
audit db: /home/user/Camelot-Ecosystem/integration/.run/camelot-voice.db (548K)
mesh node: local-node (local) trust=trusted health=healthy caps=1 addr=5ff333f77041
mesh node: gate-probe-node (remote) trust=revoked health=healthy caps=1 addr=9c4527ff94b6
```

## Benchmark
```
── Camelot x Kickbox slice benchmark (2026-08-08T10:07:14Z, host: x86_64, 4 cpus)
cold start:  gateway=336ms  node-agent=60ms  console-ready=336ms
gateway:     rss=17.9MB cpu=3.0%
node-agent:  rss=3.4MB cpu=0.3%
console:     rss=19.0MB cpu=1.2%  (python http.server dev stand-in)
hermes:      rss=67.8MB cpu=3.3%  cold-start=341ms
hermes latency: stt(700ms audio)=4.8ms tts=8.7ms
audit db:    564K · logs: 20K
turn latency (POST /v1/voice/turns, tier-1):    min=2.4ms avg=4.5ms p95=4.8ms max=25.2ms (n=20)
compute latency (POST /v1/compute, 1024-sample batch): min=1.9ms avg=37.1ms p95=49.7ms max=51.2ms (n=20)
mesh: node-job round trip (enrol->lease->dispatch->result) = 36ms; nodes=1
model routing: provider=deterministic requests=21 fallbacks=0 first-token=61ms completion=1277ms
── stack stopped; report saved to /home/user/Camelot-Ecosystem/integration/.run/benchmark.txt
```

## Manual checklist — the three items no script can do

### Item 2 — a real remote node (needs a second machine on your tailnet)
- [ ] Second machine agent enrolled; appears in Node Status as *pending*
- [ ] After POST /v1/nodes/<id>/trust it shows *trusted · ready*
- [ ] A read-only job routed to it returns a result (the route line names it)

### Item 8 — transport loss
- [ ] `tailscale down` on the remote → node degrades/offline within ~45s
- [ ] Work continues locally throughout; no errors surface to the user
- [ ] `tailscale up` → node returns (re-promote from limited if needed)

### Item 9 — voice + model in the browser
- [ ] Microphone permission DENIED → text mode remains usable, visible notice
- [ ] Silence while holding PTT → "no speech" notice, nothing submitted
- [ ] Quiet speech → low-confidence review prompt, must press Send
- [ ] Normal speech → voice turn submitted, decision card + audit entry
- [ ] Barge-in while TTS is speaking → playback stops instantly, stream cancelled
- [ ] Tier-2 spoken request → draft artifact + consumed lease in audit
- [ ] Tier-3 spoken request → blocked until visible Approve; Deny revokes
- [ ] TTS start latency after reply completes: ______ (perceived, browser)
- [ ] Barge-in stop latency while speaking: ______ (perceived — must feel immediate)
- [ ] Browser used: ______

_Verdict (fill in): PASS / FAIL — notes:_
