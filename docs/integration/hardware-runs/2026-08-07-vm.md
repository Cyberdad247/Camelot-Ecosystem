# Hardware run — 2026-08-07 on vm

Phase 2 release-gate record (docs/architecture/bootstrap-plan.md; Phase 3 prerequisite).

| Field | Value |
|---|---|
| Host | vm (Linux 6.18.5-fc-v18 x86_64) |
| CPU | Intel(R) Xeon(R) Processor @ 2.10GHz × 4 |
| RAM | 15.7 GB |
| Voice | ENABLE_HERMES_VOICE=true |
| Smoke | PASSED |

## Smoke output
```
── 1. Health
   ✔ gateway healthy
   ✔ node-agent healthy (backend: cpu)
   ✔ console + contracts served
── 2. Tier-1 read (no lease)
   ✔ allowed without lease, artifact: staging_status
── 3. Tier-2 draft (auto-lease, consumed)
   ✔ draft created under consumed lease lease-0001
── 4. Tier-3 requires confirmation
   ✔ blocked with pending lease lease-0002
   ✔ approved -> executed -> lease consumed
── 5. Barge-in revokes unused lease
   ✔ barge-in revoked lease-0003; later approval rejected (409)
── 6. Audit trail (redacted, chained)
   ✔ audit audit-0082 fetched with hash chain
── 7. Node-agent compute under signed lease
   ✔ batched compute on backend: cpu
   ✔ forged compute token rejected (403)
── 8. Hermes voice adapter (ENABLE_HERMES_VOICE=true)
   ✔ hermes healthy (stt: fixture)
   ✔ stt transcribed speech-energy audio: read staging status
   ✔ silence yields no transcript (nothing submittable)
   ✔ tts produced a WAV stream

✅ smoke passed (15 checks) — Anya Console: http://localhost:8080/kickbox/
```

## Status snapshot
```
✔ gateway  pid=2376  healthy  http://localhost:8788/healthz
✔ node-agent  pid=2398  healthy  http://localhost:8789/healthz
✔ hermes  pid=2410  healthy  http://localhost:8790/healthz
✔ console  pid=2431  healthy  http://localhost:8080/kickbox/index.html
audit db: /home/user/Camelot-Ecosystem/integration/.run/camelot-voice.db (60K)
```

## Benchmark
```
── Camelot x Kickbox slice benchmark (2026-08-07T13:42:42Z, host: x86_64, 4 cpus)
cold start:  gateway=325ms  node-agent=57ms  console-ready=326ms
gateway:     rss=12.9MB cpu=2.0%
node-agent:  rss=3.1MB cpu=0.0%
console:     rss=19.0MB cpu=1.8%  (python http.server dev stand-in)
hermes:      rss=67.7MB cpu=3.2%  cold-start=328ms
hermes latency: stt(700ms audio)=3.6ms tts=6.4ms
audit db:    72K · logs: 16K
turn latency (POST /v1/voice/turns, tier-1):    min=1.6ms avg=1.9ms p95=2.5ms max=2.5ms (n=20)
compute latency (POST /v1/compute, 1024-sample batch): min=16.8ms avg=20.3ms p95=20.9ms max=44.4ms (n=20)
── stack stopped; report saved to /home/user/Camelot-Ecosystem/integration/.run/benchmark.txt
```

## Manual checklist (fill in by hand in the browser)

- [ ] Microphone permission DENIED → text mode remains usable, visible notice
- [ ] Silence while holding PTT → "no speech" notice, nothing submitted
- [ ] Quiet speech → low-confidence review prompt, must press Send
- [ ] Normal speech → voice turn submitted, decision card + audit entry
- [ ] Barge-in while TTS is speaking → playback stops instantly, stream cancelled
- [ ] Tier-2 spoken request → draft artifact + consumed lease in audit
- [ ] Tier-3 spoken request → blocked until visible Approve; Deny revokes

_Verdict (fill in): PASS / FAIL — notes:_
