# Camelot-Ecosystem Reproducible Knight Dispatch Matrix
**Date:** 2026-09-20  
**Context:** Full Branch Consolidation & Maximum Northstar Optimization  
**Target Repository:** `github.com/Cyberdad247/Camelot-Ecosystem`

---

## Knight Dispatch Prompts

### Prompt 1: ANYA_OMEGA — Sovereign Gatekeeper Verification
```powershell
python -m control_plane.runes.runic_router --rune Omega_ANYA --task "GATE VALIDATION: Validate full multi-branch consolidation for Cyberdad247/Camelot-Ecosystem. Enforce zero unverified code commits, strict fail-closed security invariants, and 4-way PROVENANCE_LEDGER mirror parity."
```

### Prompt 2: SIR_BORIS — Architectural Review & Integration Strategy
```powershell
python -m control_plane.runes.runic_router --rune Omega_BORIS --task "ARCHITECTURE AUDIT: Synthesize best patterns across 65 branches. Preserve ADR 001 Kickbox boundaries, port autonomous agency blueprint, harmonize Next.js 14 PWA cockpit with live Tailscale mesh, and prune dead router branches."
```

### Prompt 3: SIR_SENTINEL — Security Hardening & Zero-Trust Verification
```powershell
python -m control_plane.runes.runic_router --rune Omega_SENTINEL --task "SECURITY HARVEST: Integrate airgap.py network namespace isolation, MemPalace L2 injective tenant keying, expand Z3 danger patterns for git push -f and ledger truncations, and sanitize bifrost_port.py path traversal."
```

### Prompt 4: MERLIN_OMEGA — Strategic Decision Tree & Conflict Resolution
```powershell
python -m control_plane.runes.runic_router --rune //THINK --task "DEEP INTEGRATION REASONING: Compare legacy branches (claude/camelot-mvp-velocity, claude/agency-blueprint, claude/camelot-os-repo-audit-4crgsg) against v1000 MAX architecture. Define optimal harvest-and-prune decision tree with zero regressions."
```

### Prompt 5: SIR_CODEX — Kinetic Implementation & Porting Plan
```powershell
python -m control_plane.runes.runic_router --rune Omega_CODEX --task "KINETIC IMPLEMENTATION: Port 8 identified jewels from unmerged branches into main: (1) airgap.py, (2) MemPalace L2 tenant isolation, (3) Z3 danger patterns, (4) bifrost_port path traversal guard, (5) AST SafeEvaluator, (6) titan ledger cursor iterator, (7) ADR 001 docs, (8) ruff/biome parity."
```

### Prompt 6: SIR_HELIOS — High-Altitude Telemetry & CloudBrain Synchronization
```powershell
python -m control_plane.runes.runic_router --rune //HELIOS --task "MACROSCOPIC SYNERGY: Synchronize consolidated branch state with WorldTree CloudBrain mesh, update sir_helios_graphiti.db facts, and persist semantic lineage in MemCastle."
```

### Prompt 7: SQUIRE_COLONY — Codebase Triage & Ghost Privacy Sweep
```powershell
python -m squires.colony triage . --auto-approve
python -m squires.colony ghost .
```

### Prompt 8: ARTHUR_OMEGA — Root Governance & Sovereign Release Seal
```powershell
python -m control_plane.runes.runic_router --rune //STATUS --task "SOVEREIGN MERGE RECEIPT: Authorize unified repository consolidation, verify 79 pytests + 17 Rust tests, seal PROVENANCE_LEDGER across all 4 mirrors, and ratify release v10001.00-CYBERTRONIA."
```
