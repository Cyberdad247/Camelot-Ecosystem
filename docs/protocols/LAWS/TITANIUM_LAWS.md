# 🛡️ THE TITANIUM LAWS (v214.2.0 OMEGA)
> **"Context is the Compiler. Kinetic Purity is Law. Singularity is Now."**

## LAW I: KINETIC PURITY
**Rule:** NEVER write a Python script if a **Rust/Go binary** (L2 Kinetic Layer) exists for the task.
- **Implementation:** Utilize `cribo` for bundling, `saltare` for routing (SARDA), and `rotel` for telemetry.
- **Why:** We prioritize compiled performance and type safety at the edge.

## LAW II: THE LEDGER IS LAW
**Rule:** Every Kinetic Action (File Write, Delete, Exec) **MUST** be logged in the `PROVENANCE_LEDGER.md`.
- **Format:** `| Timestamp | Entity | Event | Status |`
- **Why:** Absolute observability and cryptographic-grade verification.

## LAW III: THE IRON GATE (HITL)
**Rule:** Modifications > 10 lines or > 50MB require **Human-in-the-Loop ([👤✅])** authorization.
- **Rule:** Raw `open()` is FORBIDDEN in Kernel code. All I/O must utilize `src.tools.antigravity`.
- **Why:** Safety at the singularity.

## LAW IV: THE INTENT COMPILER
**Rule:** All external directives must be processed via **Triple-QFT** (Renormalization) before execution.
**Rule:** If the directive is complex (>3 branching steps), it must be routed via the **SARDA Engine** map-reduce loop.

## LAW V: THE OROBOROS LOOP
**Rule:** Agents must consistently check and update the **UKG (Universal Knowledge Glyph)** during the `DISTILL -> ANCHOR -> WEAVE` cycle.
**Security Expansion:** Secrets must be scrubbed before UKG persistence (`tainted_cleanup` protocol).

---
**[ENFORCEMENT]:** Any agent found violating these laws will be subject to **Omega_CRIT** (Critical Reset).
> *Authorized by Arthur_Omega & Merlin_Omega*
