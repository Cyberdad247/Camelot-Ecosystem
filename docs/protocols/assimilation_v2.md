# 🧬 ASSIMILATION PROTOCOL V2 (The Hive)
> **Guardian**: L6 (Arthur)
> **Source**: Fused from `openclaw` (Meta-Adaptation) & `pi-mono` (Swarm Governance).

## I. THE TRINITY TEST
Before assimilating ANY external pattern, it must satisfy **The Trinity**:

1.  **L2 (Kinetic Purity)**:
    *   Does it reduce friction?
    *   Is it automatable via CLI/Script?
    *   *Law*: "If it requires a mouse, it is not Kinetic."

2.  **L4 (Semantic Truth)**:
    *   Does it conflict with `UKG` (Universal Knowledge Graph)?
    *   does it duplication existing patterns?
    *   *Law*: "One Source of Truth. No Shadow Logic."

3.  **L7 (Ethereal Vibe)**:
    *   Does it "spark joy" or increase "RADIANCY"?
    *   *Law*: "Code is Art. Ugly code is technical debt."

## II. THE SWARM LOCK (Governance)
When Swarm Agents (Nano-Knights) are deployed to assimilate a target:

1.  **Strict Ownership**:
    *   Agent A triggers `git clone`.
    *   Agent B analyzes `package.json`.
    *   Agent C analyzes `security`.
    *   **Rule**: Agent B NEVER touches `security` files. Agent C NEVER touches `package.json`.

2.  **Lockstep Commits**:
    *   All changes must form a coherent atomic unit.
    *   Use `sync-versions` logic to ensure no package drifts ahead.

## III. THE META-HOOK (Self-Evolution)
*   **Definition**: The Protocol allows the System to rewrite its own Protocol.
*   **Mechanism**:
    1.  Propose Change to `assimilation_v2.md`.
    2.  Hash current state (`PROVENANCE_LEDGER.md`).
    3.  Apply & Verify.
    4.  If Failure -> Rollback to Hash.

## IV. EXECUTION
To activate this protocol:
```bash
python 02_FORGE/kinetic/knight_upgrade.py --target "assimilation_v2"
```
