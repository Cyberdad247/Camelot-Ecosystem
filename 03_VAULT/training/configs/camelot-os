#!/usr/bin/env bash
# Camelot OS -- Terminal entry point
# Usage: camelot-os [--no-hud] [--status] [--ask "question"] [-p provider]

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONIOENCODING=utf-8
exec python "$SCRIPT_DIR/hud.py" "$@"
