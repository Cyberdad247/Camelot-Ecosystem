# 📞 AUDIT: CLAUDE PHONE SYSTEM & 3CX INTEGRATION
> **Status:** AUDIT_COMPLETE (4 Shatterpoints Detected)
> **Goal:** Production Readiness for the Sovereign Autonomy Pipeline.
> **Date:** April 2026
> **Knights:** Anya (L7), Alex (Decomp), Merlin (Neural), Sir Forge (Engineering), Sir Kronos (Temporal)

## 1. Executive Summary
The "Claude Phone System" (OpenClaw `voice-call` extension) combined with the 3CX PBX bridge is architecturally sound but functionally stalled at the **Identity & Latency** layers. Our current zero-trust configuration is rejecting external SIP handshakes (401 Unauthorized), and the synchronous nature of the legacy reply-dispatcher violates **Titanium Law T8** (Sub-second latency). To reach production grade, we must transition to a **Context-Relay Asynchronous Flow**.

## 2. Technical Audit & Shatterpoints

### 🚨 Shatterpoint 1: Authorization Friction (Anya & Merlin)
*   **Issue:** The Camelot Control Plane rejects 3CX webhooks because they lack the `Authorization: Bearer` signature generated by the internal **Bifrost Gate**.
*   **Feedback:** Production readiness requires a dedicated **SIP-to-Agent** credential map. 
*   **Remediation:** Implement **Dynamic User Role-Masking**. The incoming 3CX Caller ID must be mapped to a specific Knight context with "Least Privilege" permissions.

### 🚨 Shatterpoint 2: The Velocity Breach (Sir Kronos)
*   **Issue:** The OpenClaw `voice-call` extension currently waits for a full LLM turn before responding. This results in 3-5s of dead air, exceeding the 500ms mandate.
*   **Feedback:** This is a fatal flaw for real-world telephony. 
*   **Remediation:** Shift to **Streaming Intent Ingestion**. Use the LiveKit WebRTC bridge to stream audio bytes directly to the **Lyricus Audio Engine** while the intent is still being quantized by Merlin.

### 🚨 Shatterpoint 3: Persona Soul Drifting (Alex)
*   **Issue:** The `voice-call` manager operates using generic TTS voices. This decouples the "Iron" (telephony) from the "Soul" (Knights).
*   **Feedback:** If **Sir Boris** answers the phone, he must sound like the Foundry Lead (W_orchestration=0.85), not a default OpenAI voice.
*   **Remediation:** Lock the `voice_registry.json` Voice IDs to specific OpenClaw agent profiles. Every outgoing `vocal_cue` must carry the knight's unique **Sonic Texture**.

### 🚨 Shatterpoint 4: Error Grace & Failover (Sir Forge)
*   **Issue:** There is no "Chaos Monkey" logic for telephony. A dropped SIP packet results in a generic `404` or `500`, killing the agent turn.
*   **Feedback:** Phone systems are inherently noisy; the code must be "Red-Team" hardened.
*   **Remediation:** Implement a **Contextual Interaction Brief** failover. If the agent times out, the call is routed to a human fallback with the last 30 seconds of context already transcribed and summarized.

## 3. Production-Ready Engineering Blueprint

| Layer | Component | Production Requirement |
| :--- | :--- | :--- |
| **L7 Ethereal** | `voice-call` Index | Synchronous `register()` with async `setTimeout` event emitters. |
| **L4 Semantic** | `event_bridge.py` | Implementation of `/api/v1/voice` with automatic 3CX HMAC verification. |
| **L2 Kinetic** | `rotel.exe` | High-priority telemetry tracking for audio-voltage packets. |
| **Cloud** | `tasha-voice-agent` | Deployment to Modal with `min_containers=1` to eliminate cold-start latency. |

## 4. Immediate Directives
1.  **Harden Authentication:** Create an exclusion rule in `bifrost.py` for verified 3CX IP ranges to resolve the 401 error.
2.  **Persona Binding:** Update `openclaw.json` to map specific phone numbers to **Knight Souls** (`anya_soul.txt`, `boris_soul.txt`).
3.  **Latency Compression:** Switch OpenClaw's TTS engine to **Piper (Local)** to bypass cloud network overhead for immediate "Hello" responses.

**[COUNCIL_JUDGEMENT]:** UNSTABLE BUT REDEEMABLE. 
The Spire is ready to listen, but the iron gate must be opened carefully. 

Shall I begin with **Directive 1 (Authentication Hardening)** to unblock the 3CX handshake?
