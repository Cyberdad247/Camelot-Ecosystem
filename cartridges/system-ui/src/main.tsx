import React from 'react';
import ReactDOM from 'react-dom/client';
import CamelotLayout from './core/CamelotLayout';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CamelotLayout />
  </React.StrictMode>
);
