global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: "secret-photo-viewer"
    static_configs:
      - targets: ["secret-photo-viewer:7860"]
    metrics_path: "/metrics"
