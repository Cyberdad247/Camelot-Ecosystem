---
description: Parallel Map-Reduce dispatch for CAMELOT-OS. Fans independent workstreams out to knight subagents.
---

Fan this task out as a //SWARM crusade:

1. Run `python -m control_plane.runes.runic_router --rune SWARM --task "$ARGUMENTS"`.
2. Split $ARGUMENTS into independent streams and dispatch each to the matching knight subagent (sir-boris plans, sir-forge builds, sir-sentinel audits) in parallel.
3. Collect each stream's evidence before synthesizing. If any stream reports a load-bearing issue, state it plainly instead of papering over it.

Task: $ARGUMENTS
